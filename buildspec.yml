version: 0.1

phases:
  build:
    commands:
      - echo Build started on `date`
      - echo Compiling the Go code...
      - go build Algorithm/M_5_LongestPalindromicString.go
  post_build:
    commands:
      - echo Build completed on `date`
